# Models Directory

This directory stores local trained model artifacts.

Examples of excluded files:

- `.joblib` scikit-learn models
- transformer checkpoints
- optimizer states
- other large binary outputs

For the public GitHub repository, keep this directory empty or store only small reproducible metadata.
