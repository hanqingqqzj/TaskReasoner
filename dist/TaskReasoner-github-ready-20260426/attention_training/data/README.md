# Attention Training Data

This directory contains locally prepared datasets for:

- cleaned task supervision tables
- relation supervision tables
- unlabeled corpora for continued pretraining

These generated files are excluded from the GitHub upload package because they are large and reproducible from the local TAWOS export using:

- `attention_training/prepare_attention_data.py`
